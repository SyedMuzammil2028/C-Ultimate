#include <stdio.h>

int main(){
    int a;
    printf("enter a number to check the reminder of it when divided by 97 = ");
    scanf("%d",&a);
    printf(" the reminder of is %d ",a%97);
    return 0;
}