#include <stdio.h>
// For each loop displaying array
int main()
{
    // Declaring an array of integers with 5 elements
    int array[5] = {10, 20, 30, 40, 50};

    // Accessing array elements
    // for (int ele: array)
    // {
    //     printf("%d",ele);
    // }

    return 0;
}